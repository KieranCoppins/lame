<UserControl x:Class="Lame.Frontend.Views.ExportXliffView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="1080" d:DesignWidth="720">
    <Grid Margin="32">
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <StackPanel>
            <StackPanel Style="{StaticResource SectionStyle}">
                <TextBlock Text="Export XLIFF"
                           Style="{StaticResource Header1}" />
                <TextBlock Text="Export assets to XLIFF format for translation."
                           Style="{StaticResource Subtitle}"
                           Margin="0" />
            </StackPanel>
        </StackPanel>
    </Grid>
</UserControl>