<UserControl x:Class="Lame.Frontend.Views.Dialogs.LinkAssetsDialogView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:dialogViewModels="clr-namespace:Lame.Frontend.ViewModels.Dialogs"
             xmlns:helpers="clr-namespace:Lame.Frontend.Helpers"
             xmlns:controls="clr-namespace:Lame.Frontend.Controls"
             mc:Ignorable="d"
             d:DesignHeight="300" d:DesignWidth="300"
             d:DataContext="{d:DesignInstance dialogViewModels:LinkAssetsDialogViewModel }">
    <StackPanel>
        <TextBlock Text="{Binding TitleText}"
                   Style="{StaticResource Header2}" />

        <TextBlock Text="Asset to Link"
                   Style="{StaticResource FieldLabel}" />

        <controls:DynamicLoadingComboBox Style="{StaticResource BlankComboBox}"
                                         ContentSearch="{Binding SearchAssets}"
                                         SelectedItem="{Binding SelectedAssetToLink}"
                                         Placeholder="Search for asset..." />

        <WrapPanel HorizontalAlignment="Right" Margin="0,16,0,0">
            <Button Content="Cancel"
                    Command="{Binding CancelCommand}"
                    Style="{StaticResource SecondaryButton}"
                    helpers:CommonHelpers.IsLoading="{Binding IsLinkingAsset}" />
            <Button Content="Link Asset"
                    Command="{Binding LinkAssetCommand}"
                    Style="{StaticResource PrimaryButton}" />
        </WrapPanel>

    </StackPanel>
</UserControl>