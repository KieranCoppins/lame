<Window x:Class="Lame.Frontend.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:viewModels="clr-namespace:Lame.Frontend.ViewModels"
        xmlns:enums="clr-namespace:Lame.Frontend.Enums"
        mc:Ignorable="d"
        Title="L.A.M.E" Height="450" Width="800"
        d:DataContext="{d:DesignInstance Type=viewModels:MainWindowViewModel }">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="150" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <Border Grid.Column="0"
                BorderBrush="{StaticResource Brush.LightGrey}"
                BorderThickness="0,0,1,0"
                Padding="16">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>

                <StackPanel Grid.Row="0">
                    <TextBlock Text="L.A.M.E"
                               Style="{StaticResource Header1}"
                               Margin="0" />
                    <WrapPanel>
                        <TextBlock Text="Localization Asset Management Engine"
                                   TextWrapping="Wrap"
                                   Style="{StaticResource Subtitle}"
                                   FontSize="10"
                                   Margin="0" />
                    </WrapPanel>

                    <Border Style="{StaticResource SectionLineBorderStyle}" />

                    <RadioButton Content="Dashboard"
                                 GroupName="SidebarNavigation"
                                 Style="{StaticResource NavigationButton}"
                                 IsChecked="{Binding CurrentPage, Mode=OneWay, Converter={StaticResource EnumToBooleanConverter}, ConverterParameter={x:Static enums:AppPage.Dashboard}}"
                                 Command="{Binding NavigateToDashboardCommand}" />
                    <RadioButton Content="Asset Library"
                                 GroupName="SidebarNavigation"
                                 Style="{StaticResource NavigationButton}"
                                 IsChecked="{Binding CurrentPage, Mode=OneWay, Converter={StaticResource EnumToBooleanConverter}, ConverterParameter={x:Static enums:AppPage.Library}}"
                                 Command="{Binding NavigateToLibraryCommand}" />
                    <RadioButton Content="Asset Links"
                                 GroupName="SidebarNavigation"
                                 Style="{StaticResource NavigationButton}"
                                 IsChecked="{Binding CurrentPage, Mode=OneWay, Converter={StaticResource EnumToBooleanConverter}, ConverterParameter={x:Static enums:AppPage.AssetSync}}"
                                 Command="{Binding NavigateToAssetSyncCommand}" />
                    <RadioButton Content="Create Asset"
                                 GroupName="SidebarNavigation"
                                 Style="{StaticResource NavigationButton}"
                                 IsChecked="{Binding CurrentPage, Mode=OneWay, Converter={StaticResource EnumToBooleanConverter}, ConverterParameter={x:Static enums:AppPage.CreateAsset}}"
                                 Command="{Binding NavigateToCreateAssetCommand}" />
                    <RadioButton Content="Import"
                                 GroupName="SidebarNavigation"
                                 Style="{StaticResource NavigationButton}"
                                 IsChecked="{Binding CurrentPage, Mode=OneWay, Converter={StaticResource EnumToBooleanConverter}, ConverterParameter={x:Static enums:AppPage.ImportXliff}}"
                                 Command="{Binding NavigateToImportXliffCommand}" />
                    <RadioButton Content="Export"
                                 GroupName="SidebarNavigation"
                                 Style="{StaticResource NavigationButton}"
                                 IsChecked="{Binding CurrentPage, Mode=OneWay, Converter={StaticResource EnumToBooleanConverter}, ConverterParameter={x:Static enums:AppPage.ExportXliff}}"
                                 Command="{Binding NavigateToExportXliffCommand}" />
                </StackPanel>

                <StackPanel Grid.Row="2">
                    <RadioButton Content="Settings"
                                 GroupName="SidebarNavigation"
                                 Style="{StaticResource NavigationButton}"
                                 IsChecked="{Binding CurrentPage, Mode=OneWay, Converter={StaticResource EnumToBooleanConverter}, ConverterParameter={x:Static enums:AppPage.Settings}}"
                                 Command="{Binding NavigateToSettingsCommand}" />
                    <Border Style="{StaticResource SectionLineBorderStyle}"
                            VerticalAlignment="Bottom">
                        <StackPanel>
                            <TextBlock Style="{StaticResource Subtitle}"
                                       FontSize="10"
                                       Margin="0"
                                       TextWrapping="Wrap">
                                <TextBlock.Text>
                                    <MultiBinding StringFormat="{}Version: {0}">
                                        <Binding Path="AppVersion" />
                                    </MultiBinding>
                                </TextBlock.Text>
                            </TextBlock>
                            <TextBlock Text="By Kieran Coppins"
                                       Style="{StaticResource Subtitle}"
                                       FontSize="10"
                                       Margin="0, 8" />
                        </StackPanel>
                    </Border>
                </StackPanel>
            </Grid>
        </Border>

        <Grid Grid.Column="1">

            <ContentControl Content="{Binding CurrentView}" x:Name="MainWindowContent" />

            <ContentControl
                Content="{Binding ActiveDialog}">

                <ContentControl.Style>
                    <Style TargetType="ContentControl">
                        <Setter Property="Visibility" Value="Collapsed" />
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding IsDialogOpen}" Value="True">
                                <Setter Property="Visibility" Value="Visible" />
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </ContentControl.Style>

                <ContentControl.Template>
                    <ControlTemplate TargetType="ContentControl">
                        <Grid Background="#80000000" PreviewMouseDown="DialogBackground_OnPreviewMouseDown">
                            <Border
                                Style="{StaticResource CardBorderStyle}"
                                Width="400"
                                HorizontalAlignment="Center"
                                VerticalAlignment="Center">

                                <ContentPresenter Content="{TemplateBinding Content}"
                                                  ContentTemplate="{TemplateBinding ContentTemplate}" />

                            </Border>
                        </Grid>
                    </ControlTemplate>
                </ContentControl.Template>

            </ContentControl>
        </Grid>

        <ItemsControl ItemsSource="{Binding Notifications}"
                      HorizontalAlignment="Right"
                      VerticalAlignment="Top"
                      IsHitTestVisible="False"
                      Grid.Column="1"
                      Panel.ZIndex="999"
                      Margin="0,0,16,0">
            <ItemsControl.ItemsPanel>
                <ItemsPanelTemplate>
                    <StackPanel>
                        <StackPanel.Style>
                            <Style TargetType="StackPanel">
                                <Setter Property="Margin">
                                    <Setter.Value>
                                        <Thickness Left="{StaticResource Margin.Standard}"
                                                   Top="0"
                                                   Right="{StaticResource Margin.Standard}"
                                                   Bottom="0" />
                                    </Setter.Value>
                                </Setter>
                            </Style>
                        </StackPanel.Style>
                    </StackPanel>
                </ItemsPanelTemplate>
            </ItemsControl.ItemsPanel>

            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <Border Style="{StaticResource NotificationBorder}"
                            Background="{Binding Type, Converter={StaticResource NotificationTypeToBrushConverter}}"
                            Width="300">
                        <StackPanel>
                            <TextBlock Text="{Binding Title}"
                                       Style="{StaticResource NotificationText}"
                                       Foreground="{StaticResource Brush.White}"
                                       TextWrapping="Wrap"
                                       FontWeight="Bold" />
                            <TextBlock Text="{Binding Message}"
                                       Style="{StaticResource NotificationText}"
                                       Foreground="{StaticResource Brush.White}"
                                       TextWrapping="Wrap" />
                        </StackPanel>
                    </Border>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>
    </Grid>
</Window>