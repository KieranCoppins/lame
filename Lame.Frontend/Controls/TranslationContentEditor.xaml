<UserControl x:Class="Lame.Frontend.Controls.TranslationContentEditor"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:helpers="clr-namespace:Lame.Frontend.Helpers"
             xmlns:domainModel="clr-namespace:Lame.DomainModel;assembly=Lame.DomainModel"
             xmlns:controls="clr-namespace:Lame.Frontend.Controls"
             mc:Ignorable="d"
             d:DesignHeight="300" d:DesignWidth="300">
    <UserControl.Resources>

        <!-- TEXT TEMPLATE -->
        <DataTemplate x:Key="TextTemplate">
            <TextBox Style="{StaticResource StyledInputBox}"
                     AcceptsReturn="True"
                     TextWrapping="Wrap"
                     VerticalScrollBarVisibility="Auto"
                     HorizontalScrollBarVisibility="Disabled"
                     MinHeight="60"
                     helpers:TextBoxHelpers.Placeholder="Enter UI text or label..."
                     Text="{Binding TranslationContent, 
                            RelativeSource={RelativeSource AncestorType=UserControl},
                            UpdateSourceTrigger=PropertyChanged}" />
        </DataTemplate>

        <!-- AUDIO TEMPLATE -->
        <DataTemplate x:Key="AudioTemplate">
            <controls:FileUploaderControl
                FilePath="{Binding TranslationContent, RelativeSource={RelativeSource AncestorType=UserControl}}"
                FileFilter="Audio Files|*.mp3;*.wav;*.ogg;*.flac;*.m4a;" />
        </DataTemplate>

    </UserControl.Resources>

    <Grid>
        <ContentControl>
            <ContentControl.Style>
                <Style TargetType="ContentControl">
                    <Setter Property="ContentTemplate" Value="{StaticResource TextTemplate}" />
                    <Style.Triggers>
                        <DataTrigger
                            Binding="{Binding AssetType, RelativeSource={RelativeSource AncestorType=UserControl}}"
                            Value="{x:Static domainModel:AssetType.Audio}">
                            <Setter Property="ContentTemplate" Value="{StaticResource AudioTemplate}" />
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </ContentControl.Style>
        </ContentControl>
    </Grid>
</UserControl>