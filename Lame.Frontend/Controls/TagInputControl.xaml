<UserControl x:Class="Lame.Frontend.Controls.TagInputControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:Lame.Frontend.Controls"
             mc:Ignorable="d"
             d:DesignHeight="300" d:DesignWidth="300">
    <Border
        Style="{StaticResource InputBox}"
        Background="Transparent">

        <StackPanel>

            <ItemsControl
                ItemsSource="{Binding SelectedTags, RelativeSource={RelativeSource AncestorType=UserControl}}">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <WrapPanel />
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>

                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border Style="{StaticResource ChipBorderStyle}"
                                Background="{Binding Id, Converter={StaticResource GuidToColourConverter}}">
                            <StackPanel Orientation="Horizontal">
                                <Button Style="{StaticResource ClickableText}"
                                        Click="RemoveTag_OnClick">
                                    <TextBlock Text="x"
                                               Style="{StaticResource Body}"
                                               Foreground="{StaticResource Brush.White}"
                                               Margin="0, 0, 8, 0" />
                                </Button>

                                <TextBlock Text="{Binding Name}"
                                           Style="{StaticResource Body}"
                                           VerticalAlignment="Center"
                                           Foreground="{StaticResource Brush.White}"
                                           Margin="0" />
                            </StackPanel>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>

            <local:DynamicLoadingComboBox
                x:Name="TagComboBox"
                ContentSearch="{Binding TagSearchWrapper, RelativeSource={RelativeSource AncestorType=UserControl}}"
                SelectedItem="{Binding SelectedTag, RelativeSource={RelativeSource AncestorType=UserControl}}"
                ItemTemplate="{StaticResource TextComboBoxItemTemplate}"
                Placeholder="Enter tags..."
                KeyDown="TagComboBox_OnKeyDown" />
        </StackPanel>

    </Border>
</UserControl>