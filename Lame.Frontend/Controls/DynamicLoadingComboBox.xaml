<ComboBox x:Class="Lame.Frontend.Controls.DynamicLoadingComboBox"
          xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
          xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
          xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
          xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
          xmlns:helpers="clr-namespace:Lame.Frontend.Helpers"
          mc:Ignorable="d"
          d:DesignHeight="300" d:DesignWidth="300">
    <ComboBox.Template>
        <ControlTemplate TargetType="ComboBox">
            <Grid>
                <Grid>
                    <Border x:Name="Border"
                            Style="{StaticResource InputBox}"
                            Background="{TemplateBinding Background}">
                        <ContentPresenter
                            Margin="0"
                            VerticalAlignment="Center"
                            HorizontalAlignment="Left"
                            Content="{Binding SelectionBoxItem, RelativeSource={RelativeSource AncestorType=ComboBox}}"
                            ContentTemplate="{Binding SelectionBoxItemTemplate, RelativeSource={RelativeSource AncestorType=ComboBox}}"
                            ContentStringFormat="{Binding SelectionBoxItemStringFormat, RelativeSource={RelativeSource AncestorType=ComboBox}}" />
                    </Border>

                    <TextBox x:Name="PART_EditableTextBox"
                             Style="{StaticResource BlankInput}"
                             VerticalAlignment="Center"
                             Background="Transparent"
                             Visibility="Hidden"
                             Text="{Binding SearchText, RelativeSource={RelativeSource AncestorType=ComboBox}, Delay=300, UpdateSourceTrigger=PropertyChanged}"
                             helpers:TextBoxHelpers.Placeholder="{Binding Placeholder, RelativeSource={RelativeSource AncestorType=ComboBox}}" />
                </Grid>

                <!-- Dropdown Popup -->
                <Popup x:Name="PART_Popup"
                       Placement="Bottom"
                       IsOpen="{Binding IsDropDownOpen,RelativeSource={RelativeSource AncestorType=ComboBox}}"
                       AllowsTransparency="True"
                       Focusable="False"
                       PopupAnimation="Fade">

                    <Border Background="White"
                            BorderBrush="#D1D5DB"
                            BorderThickness="1"
                            CornerRadius="8"
                            Margin="0,4,0,0"
                            MinWidth="{Binding ActualWidth, RelativeSource={RelativeSource AncestorType=ComboBox}}">

                        <ScrollViewer Margin="4"
                                      SnapsToDevicePixels="True">
                            <ItemsPresenter />
                        </ScrollViewer>
                    </Border>
                </Popup>
            </Grid>

            <!-- Focus Styling -->
            <ControlTemplate.Triggers>
                <Trigger Property="IsKeyboardFocusWithin" Value="True">
                    <Setter TargetName="Border"
                            Property="BorderBrush"
                            Value="#3B82F6" />
                    <Setter TargetName="Border"
                            Property="BorderThickness"
                            Value="3" />
                    <Setter TargetName="Border"
                            Property="Margin"
                            Value="0" />
                </Trigger>
                <Trigger Property="IsEditable" Value="True">
                    <Setter TargetName="PART_EditableTextBox" Property="Visibility" Value="Visible" />
                    <Setter TargetName="Border" Property="Visibility" Value="Hidden" />
                </Trigger>
            </ControlTemplate.Triggers>
        </ControlTemplate>
    </ComboBox.Template>
</ComboBox>