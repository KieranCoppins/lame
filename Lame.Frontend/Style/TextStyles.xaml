<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Style x:Key="Body">
        <Setter Property="TextElement.FontSize" Value="12" />
        <Setter Property="TextElement.Foreground" Value="{StaticResource Brush.Black}" />
        <Setter Property="FrameworkElement.Margin" Value="8" />
    </Style>

    <Style x:Key="Header1" BasedOn="{StaticResource Body}">
        <Setter Property="TextElement.FontSize" Value="24" />
        <Setter Property="TextElement.FontWeight" Value="Bold" />
        <Setter Property="FrameworkElement.Margin" Value="0, 8" />
    </Style>

    <Style x:Key="Header2" BasedOn="{StaticResource Body}">
        <Setter Property="TextElement.FontSize" Value="18" />
        <Setter Property="TextElement.FontWeight" Value="Bold" />
        <Setter Property="FrameworkElement.Margin" Value="0, 10" />
    </Style>

    <Style x:Key="Header3" BasedOn="{StaticResource Body}">
        <Setter Property="TextElement.FontSize" Value="14" />
        <Setter Property="TextElement.FontWeight" Value="Bold" />
        <Setter Property="FrameworkElement.Margin" Value="0, 12" />
    </Style>

    <Style x:Key="Subtitle" BasedOn="{StaticResource Body}">
        <Setter Property="TextElement.FontSize" Value="13" />
        <Setter Property="TextElement.Foreground" Value="{StaticResource Brush.DarkGrey}" />
    </Style>

    <Style x:Key="FieldLabel" BasedOn="{StaticResource Body}">
        <Setter Property="TextElement.FontSize" Value="13" />
    </Style>

    <Style x:Key="NotificationText" BasedOn="{StaticResource Body}">
        <Setter Property="TextElement.FontSize" Value="11" />
        <Setter Property="FrameworkElement.Margin" Value="0" />
    </Style>

    <Style x:Key="Link" TargetType="TextBlock" BasedOn="{StaticResource Body}">
        <Style.Triggers>
            <DataTrigger Binding="{Binding IsMouseOver, RelativeSource={RelativeSource AncestorType=Button}}"
                         Value="True">
                <Setter Property="Foreground" Value="{StaticResource Brush.Primary}" />
                <Setter Property="TextDecorations" Value="Underline" />
            </DataTrigger>
        </Style.Triggers>
    </Style>
</ResourceDictionary>